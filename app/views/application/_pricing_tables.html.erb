<div class="header-clear-medium mt-n4">
	<% if request.user_agent.include?('Mobile') %>
		<div class="pricing-4 rounded-m shadow-m bg-theme">
			  <h1 class="pricing-title text-center bg-green-dark text-uppercase">Basic</h1>
			  <h3 class="pricing-value text-center bg-green-light color-white"><%= number_to_currency(@basicPrice['unit_amount'] * 0.01, precision: 2) %></h3>
			  <h2 class="pricing-subtitle text-center bg-green-light">Per <%= @basicPrice['recurring']['interval'].titleize %></h2>
			  <ul class="pricing-list text-center">
		      <li>Demo File</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Free Install</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Product Guide</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Lifetime Updates</li>
	        <div class="divider mb-2 mt-1"></div>

			  </ul>
			  <% if current_user %>
				  <% if subscriptions.map{|d|d[:price]}.include?(@basicPrice['id']) %>
					  <p class="text-center color-gray-dark small-text font-10 uppercase mt-2 mb-0 pb-0">Your Current Plan</p>
					<% else %>
					  <a href="<%= @authBasicSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
					<% end %>
			  <% else %>
				  <a href="<%= @basicSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
			  <% end %>
			</div>
			<div class="pricing-4 rounded-m shadow-m bg-theme">
			  <h1 class="pricing-title text-center bg-green-dark text-uppercase">Executive</h1>
			  <h3 class="pricing-value text-center bg-green-light color-white"><%= number_to_currency(@bizPrice['unit_amount'] * 0.01, precision: 2) %></h3>
			  <h2 class="pricing-subtitle text-center bg-green-light">Per <%= @bizPrice['recurring']['interval'].titleize %></h2>
			  <ul class="pricing-list text-center">
		      <li>Demo File</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Free Install</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Product Guide</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Lifetime Updates</li>
	        <div class="divider mb-2 mt-1"></div>

			  </ul>
			  <% if current_user %>
			  	<% if subscriptions.map{|d|d[:price]}.include?(@bizPrice['id']) %>
					  <p class="text-center color-gray-dark small-text font-10 uppercase mt-2 mb-0 pb-0">Your Current Plan</p>
					<% else %>
					  <a href="<%= @authBizSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
					<% end %>
			  <% else %>
				  <a href="<%= @bizSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
			  <% end %>
			</div>
			<div class="pricing-4 rounded-m shadow-m bg-theme">
			  <h1 class="pricing-title text-center bg-green-dark text-uppercase">Equity &amp; Inclusion</h1>
			  <h3 class="pricing-value text-center bg-green-light color-white"><%= number_to_currency(@equityPrice['unit_amount'] * 0.01, precision: 2) %></h3>
			  <h2 class="pricing-subtitle text-center bg-green-light">Per <%= @equityPrice['recurring']['interval'].titleize %></h2>
			  <ul class="pricing-list text-center">
		      <li>Demo File</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Free Install</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Product Guide</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Lifetime Updates</li>
	        <div class="divider mb-2 mt-1"></div>

			  </ul>
			  <% if current_user %>
				  <% if subscriptions.map{|d|d[:price]}.include?(@equityPrice['id']) %>
					  <p class="text-center color-gray-dark small-text font-10 uppercase mt-2 mb-0 pb-0">Your Current Plan</p>
					<% else %>
					  <a href="<%= @authEquitySession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
					<% end %>
			  <% else %>
				  <a href="<%= @equitySession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
			  <% end %>
			</div>
  <% else %>
		<div class="row">
			<div class="pricing-4 rounded-m shadow-m bg-theme">
			  <h1 class="pricing-title text-center bg-green-dark text-uppercase">Basic</h1>
			  <h3 class="pricing-value text-center bg-green-light color-white"><%= number_to_currency(@basicPrice['unit_amount'] * 0.01, precision: 2) %></h3>
			  <h2 class="pricing-subtitle text-center bg-green-light">Per <%= @basicPrice['recurring']['interval'].titleize %></h2>
			  <ul class="pricing-list text-center">
		      <li>Demo File</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Free Install</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Product Guide</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Lifetime Updates</li>
	        <div class="divider mb-2 mt-1"></div>

			  </ul>
			  <% if current_user %>
				  <% if subscriptions.map{|d|d[:price]}.include?(@basicPrice['id']) %>
				  	<% subscriptions.each do |subscX| %>
				  		<% if subscX[:price] == @basicPrice['id'] %>
					  		<% if subscX[:active] == true %>
								  <p class="text-center color-gray-dark small-text font-10 uppercase mt-2 mb-0 pb-0">Your Current Plan</p>
					  		<% else %>
								  <a href="/resume-membership/<%= subscX[:subscription] %>" class="btn btn-s bg-blue-dark btn-center-l text-uppercase rounded-s font-900">Resume</a>
						  	<% end %>
					  	<% end %>
				  	<% end %>

					<% else %>
					  <a href="<%= @authBasicSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
					<% end %>
			  <% else %>
				  <a href="<%= @basicSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
			  <% end %>
			</div>
			<div class="pricing-4 rounded-m shadow-m bg-theme">
			  <h1 class="pricing-title text-center bg-green-dark text-uppercase">Executive</h1>
			  <h3 class="pricing-value text-center bg-green-light color-white"><%= number_to_currency(@bizPrice['unit_amount'] * 0.01, precision: 2) %></h3>
			  <h2 class="pricing-subtitle text-center bg-green-light">Per <%= @bizPrice['recurring']['interval'].titleize %></h2>
			  <ul class="pricing-list text-center">
		      <li>Demo File</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Free Install</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Product Guide</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Lifetime Updates</li>
	        <div class="divider mb-2 mt-1"></div>

			  </ul>
			  <% if current_user %>
			  	<% if subscriptions.map{|d|d[:price]}.include?(@bizPrice['id']) %>
						<% subscriptions.each do |subscX| %>
							<% if subscX[:price] == @bizPrice['id'] %>
					  		<% if subscX[:active] == true %>
								  <p class="text-center color-gray-dark small-text font-10 uppercase mt-2 mb-0 pb-0">Your Current Plan</p>
					  		<% else %>
								  <a href="/resume-membership/<%= subscX[:subscription] %>" class="btn btn-s bg-blue-dark btn-center-l text-uppercase rounded-s font-900">Resume</a>
						  	<% end %>
					  	<% end %>
				  	<% end %>
					<% else %>
					  <a href="<%= @authBizSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
					<% end %>
			  <% else %>
				  <a href="<%= @bizSession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
			  <% end %>
			</div>
			<div class="pricing-4 rounded-m shadow-m bg-theme">
			  <h1 class="pricing-title text-center bg-green-dark text-uppercase">Equity &amp; Inclusion</h1>
			  <h3 class="pricing-value text-center bg-green-light color-white"><%= number_to_currency(@equityPrice['unit_amount'] * 0.01, precision: 2) %></h3>
			  <h2 class="pricing-subtitle text-center bg-green-light">Per <%= @equityPrice['recurring']['interval'].titleize %></h2>
			  <ul class="pricing-list text-center">
		      <li>Demo File</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Free Install</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Product Guide</li>
	        <div class="divider mb-2 mt-1"></div>
		      <li>Lifetime Updates</li>
	        <div class="divider mb-2 mt-1"></div>

			  </ul>
			  <% if current_user %>
				  <% if subscriptions.map{|d|d[:price]}.include?(@equityPrice['id']) %>
					  <% subscriptions.each do |subscX| %>
					  	<% if subscX[:price] == @equityPrice['id'] %>
					  		<% if subscX[:active] == true && subscX[:price]%>
								  <p class="text-center color-gray-dark small-text font-10 uppercase mt-2 mb-0 pb-0">Your Current Plan</p>
					  		<% else %>
								  <a href="/resume-membership/<%= subscX[:subscription] %>" class="btn btn-s bg-blue-dark btn-center-l text-uppercase rounded-s font-900">Resume</a>
						  	<% end %>
					  	<% end %>
				  	<% end %>
					<% else %>
					  <a href="<%= @authEquitySession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
					<% end %>
			  <% else %>
				  <a href="<%= @equitySession['url'] %>" class="btn btn-s bg-green-dark btn-center-l text-uppercase rounded-s font-900">Join</a>
			  <% end %>
			</div>
		</div>
  <% end %> 
</div>
